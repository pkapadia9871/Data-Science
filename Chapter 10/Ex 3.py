##This is a shorter version of the given question:
##We will have the user input a string and convert certain letters of the alphabet
##to special characters for encryption
##Here we will not use a text file, however, due to the tragic ignorance of Chapter 7.

def main():

        alpha_code = {'A': '%', 'a' : '9', 'B' : '@', 'b' : '#'}

        stringin = raw_input('Enter a string: ')

        l = len(alpha_code)

        for x in stringin:
            
            if x == index:
                
                x == alpha_code[index]
                
        print stringin
        
main()
