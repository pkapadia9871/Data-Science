def main():

    string_ = input('Enter a string: ')

    ##occ is a variable holding the number of occurrences of a number
    occ = 0

    for index in range(len(string_))
        for i in range(len(string_))
            if string(index) == string(i) and i != index
                
